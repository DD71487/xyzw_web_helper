/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * Web 环境：使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);
      return await this.requestWebWakeLock();
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理 WakeLock 资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);
      await this.releaseWebWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      console.warn("[WakeLock] 提示: 请使用Chrome/Edge浏览器,并确保是HTTPS或localhost环境");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");
      console.log("[WakeLock] 锁状态:", this.wakeLock.released ? "已释放" : "活跃中");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      // 当页面从隐藏变为可见时，重新请求 WakeLock（因为页面隐藏时锁会自动释放）
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.error("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          } else if (document.visibilityState === "hidden") {
            console.log("[WakeLock] 页面隐藏,WakeLock可能失效");
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      console.error("[WakeLock] Web WakeLock请求失败:", error.message);
      console.error("[WakeLock] 错误名称:", error.name);

      // 根据错误类型给出具体提示
      if (error.name === "NotAllowedError") {
        console.error("[WakeLock] 权限被拒绝,请确保:");
        console.error("[WakeLock]   1. 使用HTTPS或localhost");
        console.error("[WakeLock]   2. 用户有交互操作(点击页面)");
      } else if (error.name === "NotSupportedError") {
        console.error("[WakeLock] 浏览器不支持WakeLock API");
      }

      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      await this.wakeLock.release();
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 获取当前状态
   * @returns {Object} 当前状态对象
   */
  getStatus() {
    return {
      isActive: this.isActive,
      env: this.env,
      hasWakeLock: this.wakeLock !== null,
      wakeLockReleased: this.wakeLock ? this.wakeLock.released : true,
    };
  }
}

// 创建单例实例
const wakeLockManager = new WakeLockManager();

export { WakeLockManager, wakeLockManager };
export default wakeLockManager;/**
 */**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 */**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    ///**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   */**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock]/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.request/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock]/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   */**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠,/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      ///**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:",/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    ///**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !(/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock]/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        }/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError")/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案"/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        ///**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 No/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.no/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px"/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleep/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointer/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        ///**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,GkXfo0AgQoaBAUL3gQFC8oEEQvOBCEKCQAR3ZWJtQ1CBGQEuAAE/AQACAAAQAIAAAwAEAAAQAIAA/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,GkXfo0AgQoaBAUL3gQFC8oEEQvOBCEKCQAR3ZWJtQ1CBGQEuAAE/AQACAAAQAIAAAwAEAAAQAIAAAwAEAAAQAIAAAwAEAAAQAIAAAwAEAAAQAIAAAwAEAAAQAIAAAwAEAAAQAIAAAwAEAAAQAIAAAwAEAAAQAIAAAwAEAAAQAIAAAwAE/**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,**
 * 跨平台屏幕唤醒锁管理器
 * Web 环境专用版本（基于 Navigator Wake Lock API）
 * 防止设备在运行任务时进入休眠状态
 */

/**
 * 屏幕唤醒锁管理器
 * 支持 Web 浏览器的 Wake Lock API，带降级方案
 */
class WakeLockManager {
  constructor() {
    // 当前运行环境
    this.env = "web";
    // 防休眠是否已激活
    this.isActive = false;
    // Web WakeLock 对象引用
    this.wakeLock = null;
    // 页面可见性变化监听器
    this.visibilityHandler = null;
    // 降级方案：NoSleep.js 模拟视频播放
    this.noSleepVideo = null;
    // 降级方案：定时器ID
    this.fallbackIntervalId = null;

    console.log(`[WakeLock] 初始化完成, 当前环境: ${this.env}`);
  }

  /**
   * 请求防休眠
   * 优先使用 Wake Lock API，不支持时使用降级方案
   * @returns {Promise<boolean>} 是否成功激活
   */
  async request() {
    // 如果已经激活，直接返回成功
    if (this.isActive) {
      console.log("[WakeLock] 防休眠已处于激活状态");
      return true;
    }

    try {
      console.log(`[WakeLock] 请求防休眠, 环境: ${this.env}`);

      // 首先尝试使用原生 Wake Lock API
      const wakeLockSuccess = await this.requestWebWakeLock();
      if (wakeLockSuccess) {
        return true;
      }

      // 如果 Wake Lock 失败，尝试降级方案
      console.log("[WakeLock] Wake Lock API 不可用，尝试降级方案...");
      const fallbackSuccess = await this.requestFallbackWakeLock();
      if (fallbackSuccess) {
        this.isActive = true;
        console.log("[WakeLock] 降级方案已启用");
        return true;
      }

      return false;
    } catch (error) {
      console.error("[WakeLock] 请求失败:", error);
      return false;
    }
  }

  /**
   * 释放防休眠
   * 清理所有资源和事件监听器
   */
  async release() {
    // 如果未激活，无需释放
    if (!this.isActive) {
      console.log("[WakeLock] 防休眠未激活,无需释放");
      return;
    }

    try {
      console.log(`[WakeLock] 释放防休眠, 环境: ${this.env}`);

      // 释放原生 Wake Lock
      await this.releaseWebWakeLock();

      // 释放降级方案
      this.releaseFallbackWakeLock();

      // 重置激活状态
      this.isActive = false;

      // 移除页面可见性监听器
      if (this.visibilityHandler && typeof document !== "undefined") {
        document.removeEventListener("visibilitychange", this.visibilityHandler);
        this.visibilityHandler = null;
      }
    } catch (error) {
      console.error("[WakeLock] 释放失败:", error);
    }
  }

  /**
   * 请求 Web 平台屏幕唤醒锁
   * 使用 Navigator Wake Lock API
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestWebWakeLock() {
    // 检测浏览器是否支持 WakeLock API
    if (typeof navigator === "undefined" || !("wakeLock" in navigator)) {
      console.warn("[WakeLock] 当前浏览器不支持WakeLock API");
      return false;
    }

    try {
      console.log("[WakeLock] 请求Web Screen WakeLock...");

      // 请求屏幕唤醒锁
      this.wakeLock = await navigator.wakeLock.request("screen");
      this.isActive = true;

      console.log("[WakeLock] Web WakeLock已成功启用");

      // 监听锁被系统释放的事件
      this.wakeLock.addEventListener("release", () => {
        console.log("[WakeLock] WakeLock已被系统释放");
      });

      // 注册页面可见性变化监听器
      if (typeof document !== "undefined") {
        this.visibilityHandler = async () => {
          if (document.visibilityState === "visible" && this.isActive) {
            try {
              console.log("[WakeLock] 页面变为可见,重新请求WakeLock...");
              this.wakeLock = await navigator.wakeLock.request("screen");
              console.log("[WakeLock] 重新请求WakeLock成功");
            } catch (error) {
              console.warn("[WakeLock] 重新请求WakeLock失败:", error.message);
            }
          }
        };

        document.addEventListener("visibilitychange", this.visibilityHandler);
      }

      return true;
    } catch (error) {
      // 权限被拒绝（iframe、非HTTPS等），静默处理
      if (error.name === "NotAllowedError") {
        console.warn("[WakeLock] WakeLock权限被拒绝，将使用降级方案");
      } else {
        console.warn("[WakeLock] WakeLock请求失败:", error.message);
      }
      return false;
    }
  }

  /**
   * 释放 Web 平台屏幕唤醒锁
   */
  async releaseWebWakeLock() {
    if (this.wakeLock) {
      try {
        await this.wakeLock.release();
      } catch (e) {
        // 忽略释放错误
      }
      this.wakeLock = null;
      console.log("[WakeLock] Web WakeLock已释放");
    }
  }

  /**
   * 降级方案：使用隐藏视频播放防止休眠
   * 模拟 NoSleep.js 的实现原理
   * @returns {Promise<boolean>} 是否成功启用
   */
  async requestFallbackWakeLock() {
    try {
      // 创建隐藏的视频元素
      if (!this.noSleepVideo) {
        this.noSleepVideo = document.createElement("video");
        this.noSleepVideo.setAttribute("playsinline", "");
        this.noSleepVideo.setAttribute("muted", "");
        this.noSleepVideo.setAttribute("loop", "");
        this.noSleepVideo.style.position = "absolute";
        this.noSleepVideo.style.top = "-10000px";
        this.noSleepVideo.style.left = "-10000px";
        this.noSleepVideo.style.width = "1px";
        this.noSleepVideo.style.height = "1px";
        this.noSleepVideo.style.opacity = "0";
        this.noSleepVideo.style.pointerEvents = "none";

        // 使用一个极小的透明视频数据URI
        // 这是一个1x1像素的透明WebM视频
        const transparentVideo =
          "data:video/webm;base64,